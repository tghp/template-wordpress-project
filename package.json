{
  "name": "$tghp:lowerCaseHyphenated$-project",
  "version": "1.0.0",
  "description": "$tghp:standard$ WordPress Project",
  "author": "TGHP",
  "type": "module",
  "scripts": {
    "dev": "HMR=$(dotenv -p VITE_HMR); PORT=$(dotenv -p VITE_PORT); [[ $HMR = 1 && $PORT > 1 ]] && vite dev --port \"$PORT\" || npm run watch",
    "develop": "npm run dev",
    "watch": "vite build --mode watch",
    "build": "vite build"
  },
  "devDependencies": {
    "@tghp/vitepress": "2.3.0",
    "dotenv-cli": "^6.0.0"
  },
  "dependencies": {
    "@tghp/groundwork.js": "^2.3.1",
    "@tghp/prettier-config": "^1.1.0",
    "prettier": "^3.6.2",
    "the-new-css-reset": "^1.4.1",
    "sass-mq": "^7.0.0"
  }
}
